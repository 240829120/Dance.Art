<UserControl x:Class="Dance.Art.Module.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dance.Art.Module"
             xmlns:dance="http://schemas.dance.com/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance local:WelcomeViewModel}"
             dance:DanceCommandTrigger.LoadedOnceCommand="{Binding Path=LoadedCommand}">
    <Grid>
        <Image Source="/Dance.Art.Module;component/Themes/Resources/Images/bg.png" Stretch="UniformToFill"></Image>
        <Grid VerticalAlignment="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"></RowDefinition>
                <RowDefinition Height="20"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Foreground="White" Text="{Binding Path=ProgressMessage}" Margin="10,0,10,0" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"></TextBlock>
            <ProgressBar Height="20" VerticalAlignment="Bottom" Grid.Row="1"
                         Minimum="0" Maximum="1" Value="{Binding Path=ProgressValue}"></ProgressBar>
        </Grid>
    </Grid>
</UserControl>
